import { useState } from 'react'; import { CtaBand, PageHead } from '../components/ui';
export default function GalleryPage({ go, content }) { const [filter, setFilter] = useState('All'); const items = content?.gallery || []; const types = ['All', ...new Set(items.map(x => x.type || 'Other'))]; return <><PageHead crumb="Gallery" title={<>Our <em>Gallery</em></>} desc="A closer look at our products, project support and deliveries."/><section className="gallery"><div className="tabs">{types.map(x => <button key={x} className={filter === x ? 'selected' : ''} onClick={() => setFilter(x)}>{x}</button>)}</div><div>{items.filter(x => filter === 'All' || x.type === filter).map(x => <figure key={x.id}><img src={x.image} alt={x.title}/><figcaption>{x.title}</figcaption></figure>)}</div></section><CtaBand go={go}/></>; }
