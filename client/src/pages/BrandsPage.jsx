import { useState } from 'react'; import { PageHead } from '../components/ui';
export default function BrandsPage({ content }) { const [term, setTerm] = useState(''); const brands = (content?.brands || []).filter(x => x.name.toLowerCase().includes(term.toLowerCase())); return <><PageHead crumb="Brands" title={<>Brands We <em>Trust</em></>} desc="Products from leading manufacturers, sourced for professional and industrial use."/><section className="brands"><input className="brand-search" placeholder="Search brands..." value={term} onChange={e => setTerm(e.target.value)}/><div className="brand-grid">{brands.map(x => <article key={x.id}><img src={x.logo} alt={x.name}/><b>{x.name}</b><span>{x.category} · {x.products} products</span><small>{x.description}</small></article>)}</div></section></>; }
