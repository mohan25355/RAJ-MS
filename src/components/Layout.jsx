import { useEffect, useState } from 'react';
import { Menu, Search, ChevronRight, X, Home, Package, FolderKanban, Image as ImageIcon, FileText, Boxes, Phone, Users } from 'lucide-react';

export function Logo() { return <a className="logo" href="#home"><b>RAJA</b><span>ELECTRICALS 'N' HARDWARE<br />RK INNOVATIONS</span></a>; }
const links = [['Home', 'home'], ['About Us', 'about'], ['Products', 'products'], ['Projects', 'projects'], ['Gallery', 'gallery'], ['Brands', 'brands'], ['Contact Us', 'contact']];
export function Header({ go }) { const [open, setOpen] = useState(false); useEffect(() => { const showMenu = () => setOpen(true); window.addEventListener('openmenu', showMenu); return () => window.removeEventListener('openmenu', showMenu); }, []); const navigate = (event, page) => { event.preventDefault(); go(page); setOpen(false); }; return <><header><Logo /><nav>{links.map(([label, page]) => <a key={page} href={`#${page}`} onClick={event => navigate(event, page)}>{label}</a>)}<Search size={16} /><a className="quote" href="#contact" onClick={event => navigate(event, 'contact')}>Get a quote</a></nav><button className="menu" aria-label="Open menu" onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button></header>{open && <aside className="drawer"><div className="drawer-head"><Logo /><button className="drawer-close" onClick={() => setOpen(false)}><X size={18} /> Back to site</button></div>{links.map(([label, page], index) => { const Icon = [Home, Users, Package, FolderKanban, ImageIcon, Boxes, Phone][index]; return <a key={page} href={`#${page}`} onClick={event => navigate(event, page)}><Icon size={17} />{label}<ChevronRight size={14} /></a>; })}<a className="quote" href="#contact" onClick={event => navigate(event, 'contact')}>Get a quote</a><div className="drawer-stats"><b>25+<small>Years of Trust</small></b><b>5000+<small>Products</small></b><b>2000+<small>Happy Clients</small></b></div></aside>}</>; }
export function Footer({ go }) { return <footer><Logo /><div><b>Quick links</b>{[['aboutus', 'About Us'], ['products', 'Products'], ['projects', 'Projects'], ['gallery', 'Gallery']].map(([page, label]) => <button key={page} onClick={() => go(page)}>{label}</button>)}</div><div><b>Contact us</b><span>+91 99413 36125</span><span>sales@rkinnovations.com</span><span>Chennai, Tamil Nadu</span></div><small>© 2026 Raja Electricals 'N' Hardware</small></footer>; }
export function BottomNav({ go }) { return <div className="bottom-nav"><button onClick={() => go('home')}><Home />Home</button><button onClick={() => go('products')}><Package />Products</button><button onClick={() => go('projects')}><FolderKanban />Projects</button><button onClick={() => go('contactus')}><FileText />Quote</button><button onClick={() => window.dispatchEvent(new Event('openmenu'))}><Menu />Menu</button></div>; }
